#!/bin/sh

echo "downloading minikube version $1"

curl -Lo minikube https://storage.googleapis.com/minikube/releases/$1/minikube-darwin-amd64 \
  && chmod +x minikube \
  && sudo mv minikube /usr/local/bin/

